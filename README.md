# Проект: Предсказание стоимости квартир на основе данных CIAN

## 📋 Описание проекта

Проект по разработке модели машинного обучения для предсказания стоимости квартир в Питере на основе данных с сайта CIAN (Kaggle). Включает полный цикл анализа данных: от исследования и очистки до построения и сравнения различных моделей машинного обучения.

## 📊 Исходные данные

Набор данных содержит 1080 записей о квартирах со следующими характеристиками:

### Структура данных
- **1080 записей** о квартирах
- **9 признаков**, включая целевой

### Описание признаков

| Признак | Тип данных | Описание |
|---------|------------|----------|
| Underground Times | object | Информация о ближайшем метро и времени до него |
| Flat Status (1 - handed over, 0 - not handed over) | int64 | Статус квартиры (1 - сдана, 0 - не сдана) |
| Flat Type | object | Тип квартиры |
| Total Area (m^2) | float64 | Общая площадь квартиры |
| Living Area (m^2) | float64 | Жилая площадь |
| Kitchen Area (m^2) | float64 | Площадь кухни |
| Floor | int64 | Этаж |
| Year of building | int64 | Год постройки дома |
| Price (Rubles) | int64 | Цена квартиры (целевая переменная) |

## 🔍 Предобработка данных

### Выполненные этапы предобработки

1. **Очистка от выбросов**
   - Удалены аномальные значения в целевой переменной (Price)

2. **Извлечение признаков из поля "Underground Times"**
   - Создан новый признак **"min_metro_distance"** - минимальное расстояние до метро в метрах
   - Извлечено время до метро в минутах
   - Определен тип транспорта (пешком/на машине)

3. **Обработка категориальных признаков**
   - Приведение типов квартир к шаблону

4. **Создание новых признаков**
   - Выделение в новую переменную минимального времени до метро

## 📈 Анализ данных

### Корреляция признаков с ценой

| Признак | Коэффициент корреляции |
|---------|------------------------|
| Total Area (m^2) | 0.799 |
| Living Area (m^2) | 0.664 |
| Kitchen Area (m^2) | 0.626 |
| **min_metro_distance** | **-0.412** |
| Flat Status | 0.256 |
| Year of building | -0.075 |
| Floor | -0.067 |

### Ключевые наблюдения
- Площади квартиры имеют сильную положительную корреляцию с ценой
- **Расстояние до метро имеет умеренную отрицательную корреляцию (-0.412)**
- Время до метро имеет умеренную отрицательную корреляцию
- Год постройки и этаж слабо коррелируют с ценой

## 🤖 Модели машинного обучения

### Используемые алгоритмы
1. **Линейная регрессия** - базовая линейная модель
2. **Ridge регрессия** - линейная модель с L2-регуляризацией
3. **Lasso регрессия** - линейная модель с L1-регуляризацией
4. **Случайный лес** (Random Forest) - ансамблевый метод на основе деревьев
5. **Градиентный бустинг** (Gradient Boosting) - последовательный ансамблевый метод
6. **XGBoost** - оптимизированная реализация градиентного бустинга

### Метрики оценки
- **R²** (коэффициент детерминации) - чем ближе к 1, тем лучше
- **MSE** (среднеквадратичная ошибка) - чем меньше, тем лучше
- **RMSE** (корень из MSE) - ошибка в единицах целевой переменной
- **MAE** (средняя абсолютная ошибка) - менее чувствительна к выбросам
- **Кросс-валидация** - оценка устойчивости модели

## 📊 Результаты

### Сравнение производительности моделей (с учетом расстояния до метро)

| Модель | R² | RMSE | MAE | CV R² Mean |
|--------|-----|------|-----|------------|
| Random Forest | 0.721 | 1.89e+07 | 1.02e+07 | 0.735 |
| Gradient Boosting | 0.698 | 2.01e+07 | 1.08e+07 | 0.712 |
| XGBoost | 0.684 | 2.08e+07 | 1.12e+07 | 0.701 |
| Ridge Regression | 0.673 | 2.16e+07 | 1.18e+07 | 0.689 |
| Lasso Regression | 0.671 | 2.17e+07 | 1.19e+07 | 0.687 |
| Linear Regression | 0.670 | 2.18e+07 | 1.20e+07 | 0.685 |

### Ключевые выводы
1. **После добавления признака расстояния до метро, tree-based модели показывают лучшие результаты**
2. Random Forest демонстрирует наилучшую производительность (R² = 0.721)
3. Признак расстояния до метро оказался важным предиктором стоимости квартиры
4. Линейные модели показывают хорошие, но несколько худшие результаты

## 🛠 Техническая реализация

### Основные технологии
- **Python 3.8+** - язык программирования
- **Pandas** - обработка и анализ данных
- **NumPy** - научные вычисления
- **Scikit-learn** - машинное обучение
- **Matplotlib/Seaborn** - визуализация данных
- **Jupyter Notebook** - интерактивная среда разработки
- **XGBoost** - градиентный бустинг

### Пример кода для извлечения расстояния до метро

```python
import numpy as np

# Проверка корректности числовых столбцов
def check_numeric_columns(df):
    
    # Проверка корректности площадей
    invalid_area = df[
        (df['Living Area (m^2)'] > df['Total Area (m^2)']) | 
        (df['Kitchen Area (m^2)'] > df['Total Area (m^2)']) |
        (df['Living Area (m^2)'] + df['Kitchen Area (m^2)'] > df['Total Area (m^2)'])
    ]
    
    if len(invalid_area) > 0:
        print(f"Найдено {len(invalid_area)} строк с некорректными соотношениями площадей")
        display(invalid_area[area_columns])
    
    # Проверка этажа
    invalid_floor = df[df['Floor'] < 0]
    if len(invalid_floor) > 0:
        print(f"Найдено {len(invalid_floor)} строк с некорректным этажом (< 0)")
        display(invalid_floor[['Floor']])
    
    # Проверка года постройки
    invalid_year = df[
        (df['Year of building'] < 1704)
    ]
    if len(invalid_year) > 0:
        print(f"Найдено {len(invalid_year)} строк с некорректным годом постройки")
        display(invalid_year[['Year of building']])
    
    # Проверка цены
    invalid_price = df[df['Price (Rubles)'] <= 0]
    if len(invalid_price) > 0:
        print(f"Найдено {len(invalid_price)} строк с некорректной ценой (<= 0)")
        display(invalid_price[['Price (Rubles)']])

check_numeric_columns(df_clean)
```

## 🔮 Перспективы развития


1. **Feature Engineering**
   - Создание новых признаков на основе расстояния до метро
   - Взаимодействие признаков (например, площадь × расстояние до метро)
   - Нелинейные преобразования признаков

2. **Улучшение моделей**
   - Настройка гиперпараметров
   - Ансамблирование моделей
   - Эксперименты с нейронными сетями
